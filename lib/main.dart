import 'package:flutter/material.dart';

void main() {
  var title = 'update with input';
  runApp(MaterialApp(
    title: title,
    home: Scaffold(
      appBar: AppBar(
        title: Text(title),
      ),
//      body: Counter(),
    ),
  ));
}}
