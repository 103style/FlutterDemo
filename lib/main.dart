import 'package:flutter/material.dart';

void main() {
  var title = '';
  runApp(MaterialApp(
    title: title,
    home: Scaffold(
      appBar: AppBar(
        title: Text(title),
      ),
//      body: ,
    ),
  ));
}